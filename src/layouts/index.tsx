import React from 'react';
import { Button } from 'antd';

const BaseLayout: React.FC = () => {
    return (
        <div className="App">
            <Button type="primary">hello kisure</Button>
        </div>
    );
};

export default BaseLayout;